.splash
  text-align center

  #omphaloskepsis
    font-family 'Alegreya', serif
    padding 1em 0

.dashboard
  .cards
    display grid
    gap 1em
    grid-template-columns repeat(auto-fill, minmax(min(100%, 15em), 1fr))

    .card
      textarea
        width 100%
        height 7em
